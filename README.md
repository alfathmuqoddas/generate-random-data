# Shell Script for generating random json data

## Usage
```$ ./generate_random_json.sh <number of json object to be generated>```

Don't forget to make the script executable
```chmod u+x generate_random_json.sh```
